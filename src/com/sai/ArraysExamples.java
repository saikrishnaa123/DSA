/*
package com.sai;

import java.util.ArrayList;
import java.util.List;

public class ArraysExamples {
    void arrayUnion(int[] arr1, int[] arr2){

      //  List<Integer> arrList1 = ArraysExamples.(arr1);

       // List<Integer> arrList2 = ArraysExamples.toList(arr2);

        List<Integer> unionList = new ArrayList<>(arrList1);

        unionList.addAll(arrList2);

        int[] unionArr = unionList.stream().distinct().mapToInt(Integer::intValue).toArray();




    }

    static void reverse(int[] arr){
        int start =  0;
        int end = arr.length - 1;
        while(start < end){
            swap(arr, end, start);
            start++;
            end--;
        }

        ArrayList
    }

    private static void swap(int[] arr, int end, int start) {
        int temp = arr[end];
        arr[end] = arr[start];
        arr[start] = temp;
    }
}
*/
